# 🛠️ التقنيات والأدوات المستخدمة في النظام الخلفي (Backend Tech Stack)

تم بناء وتطوير النظام الخلفي (Backend) لمنصة **Jobito** باستخدام بنية برمجية متطورة تدمج بين السرعة، الأمان، والقدرة على التوسع الفوري. يعتمد النظام بالكامل على إطار العمل **NestJS** وخدمات وقواعد بيانات نشطة يتم توظيفها كالتالي:

---

## 1. إطار العمل الأساسي ولغة البرمجة (Core Architecture)

* **NestJS (v11.x)**: الإطار الهيكلي الأساسي لبناء السيرفر. يعتمد على هندسة البرمجيات القائمة على الوحدات (Modular Architecture)، مما يضمن فصل المهام وسهولة التوسع والصيانة.
* **TypeScript (v5.7)**: لغة البرمجة الأساسية في المشروع؛ لضمان كتابة كود خالي من الأخطاء أثناء التطوير (Type-Safe Coding) وتسهيل صيانة النظام.
* **SWC Compiler**: مترجم فائق السرعة مبني بلغة Rust، يُستخدم كبديل للمترجم التقليدي لتسريع عمليات تشغيل وبناء (Build) السيرفر.

---

## 2. قواعد البيانات وإدارة البيانات (Databases & ORMs)

يعتمد النظام على بيئة هجينة (Hybrid Database Architecture) تجمع بين البيانات العلاقاتية وغير العلاقاتية لتحقيق أقصى كفاءة:

* **PostgreSQL**: قاعدة البيانات العلاقاتية (Relational DB) الأساسية المعتمد عليها لحفظ الجداول الرئيسية التي تتطلب علاقات متينة ودقة عالية (مثل بيانات المستخدمين، الوظائف، والشركات).
* **TypeORM**: الـ ORM (Object-Relational Mapping) الفعلي المستخدم لإدارة جداول قاعدة بيانات PostgreSQL والتعامل مع الكيانات والعلاقات البرمجية المعقدة بسهولة.
* **MongoDB**: قاعدة البيانات غير العلاقاتية (NoSQL) المخصصة لإدارة الوثائق المرنة وسجلات المحادثات والملفات الشخصية المتغيرة.
* **Mongoose**: الـ ODM (Object Document Mapper) المستخدم لإدارة مستندات وجداول قاعدة بيانات MongoDB بكفاءة وسلاسة.

---

## 3. الاتصالات اللحظية والفورية (Real-time Communication)

* **Socket.io (WebSockets)**: البروتوكول والتقنية الفعّالة المستخدمة لتوفير اتصال فوري ثنائي الاتجاه بين الخادم والمستخدم (Real-time Bi-directional Communication)، ويتم توظيفها في إرسال الإشعارات والرسائل ومؤشر الكتابة (Typing Indicator) لحظياً.

---

## 4. الحماية والمصادقة (Authentication & Security)

* **Passport.js & JWT (JSON Web Tokens)**: النظام الأساسي لتأمين وحماية المسارات (Auth Guards) وتوثيق هوية المستخدمين عبر إصدار رموز مشفرة وآمنة.
* **Bcryptjs**: مكتبة تشفير كلمات المرور باستخدام خوارزميات تشفير قوية للغاية لمنع أي محاولة وصول غير مصرح بها للبيانات الحساسة.
* **Google OAuth 2.0 (Google Auth Library)**: تقنية المصادقة الخارجية التي تتيح للمستخدمين تسجيل الدخول الفوري والآمن باستخدام حسابات جوجل الرسمية.

---

## 5. قنوات التواصل والإشعارات الخارجية (Communication Channels)

* **Brevo HTTP API**: الواجهة السحابية المعتمدة لإرسال جميع رسائل البريد الإلكتروني للنظام (مثل رسائل التحقق OTP، روابط استعادة كلمة المرور، وتقارير النظام).
* **Firebase Admin SDK (FCM)**: يُستخدم لإجراء عمليتين حيويتين:
  1. التحقق وتأكيد أرقام الهواتف المحمولة وتفعيل الحسابات (Phone OTP Verification).
  2. إرسال الإشعارات الفورية للهواتف الذكية (Push Notifications).
* **Web-Push Protocol**: يُستخدم لإرسال الإشعارات الفورية لمتصفحات الويب (Web Push) باستخدام مفاتيح التشفير القياسية (VAPID Keys).

---

## 6. الذكاء الاصطناعي والشات بوت (AI Integration)

* **Python AI Chatbot Service**: خدمة ذكاء اصطناعي خارجية مبنية بلغة بايثون. يتصل بها خادم NestJS للرد على استفسارات المستخدمين بشكل فوري وتفاعلي، مع دعم ميزة تدفق البيانات المستمر (Streaming response via SSE).

---

## 7. إدارة الملفات وأدوات الدعم (Utilities & System Support)

* **Multer (File Storage)**: مكتبة إدارة وتخزين الملفات، وتُستخدم لمعالجة وحفظ السير الذاتية، ومستندات تفعيل الشركات، والصور الشخصية محلياً في مجلد مخصص وآمن (`/uploads`).
* **Winston Logger**: نظام تسجيل وحفظ تقارير أخطاء التشغيل والتحذيرات اللحظية للمراقبة الدورية لسلامة الخادم.
* **NestJS Throttler**: أداة لحماية الخادم من هجمات التكرار العشوائي (Rate Limiting) وتحديد سقف آمن لعدد طلبات الـ API المسموح بها في الدقيقة (حماية ضد هجمات الـ DDoS).
* **NestJS Schedule**: أداة مخصصة لجدولة وإدارة المهام التلقائية التي تعمل في الخلفية دورياً (Cron Jobs).
